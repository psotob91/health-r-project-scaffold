# Server Code Root

This folder represents the offline server code destination.

In a real project, copy the contents of:

```text
project-mirror/server_payload/
```

into this code root as one unit.
