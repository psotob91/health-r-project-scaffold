# Server Payload

This folder is the only server-transferable unit for the minimal mirror example.

Validation status: `WARNING_PENDING_HUMAN_DECISION`.

This public example contains no raw data, private metadata, package inventory, uploaded evidence, or generated analysis outputs.

Future production scripts must be added only under `scripts/`.
